{% import "triton/kernels/_common.py.jinja" as common with context %}
{% import "triton/kernels/_elementwise.py.jinja" as elementwise with context %}
{% set ctx = elementwise_binary_context(model) %}
# generated from PyNTT Jinja ElementwiseBinary.py.jinja
# {{ model["Comment"] }}; op={{ model["Op"] }}, lhs_dtype={{ model["LhsDType"] }}, rhs_dtype={{ model["RhsDType"] }}, output_dtype={{ model["OutputDType"] }}, shape={{ shape_tuple(model["Shape"]) }}
{{ "@triton.jit(noinline=True)" if model["NoInline"] else "@triton.jit" }}
def {{ model["FunctionName"] }}({{ helper_parameters(model) }}):
    shard_index = tl.program_id(0).to(tl.int64)
{{ common.shard_coordinates([model["Lhs"], model["Rhs"], model["Output"]]) }}
{% if local_buffer(model["Lhs"]) is none %}
    lhs = {{ ptr(model, "Lhs") }}
{% endif %}
{% if local_buffer(model["Rhs"]) is none %}
    rhs = {{ ptr(model, "Rhs") }}
{% endif %}
{% if local_buffer(model["Output"]) is none %}
    output = {{ ptr(model, "Output") }}
{% endif %}
{% call(indent) elementwise.iterate(ctx) %}
{{ indent }}value0 = tl.load({{ access_pointer(model, "Lhs", "lhs", ctx["lhs_access"]) }}, mask=mask, other=0.0)
{{ indent }}value1 = tl.load({{ access_pointer(model, "Rhs", "rhs", ctx["rhs_access"]) }}, mask=mask, other=0.0)
{{ indent }}result = {{ model["BinaryExpression"] }}
{{ indent }}tl.store({{ access_pointer(model, "Output", "output", ctx["output_access"]) }}, {{ "(result).to(tl.uint8)" if is_bool_dtype(model["OutputDType"]) else "result" }}, mask=mask)
{% endcall %}
